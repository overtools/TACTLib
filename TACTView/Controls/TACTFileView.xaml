<TabItem x:Class="TACTView.Controls.TACTFileView"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
         xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
         xmlns:converters="clr-namespace:TACTView.Converters"
         mc:Ignorable="d"
         DataContext="{Binding RelativeSource={RelativeSource Self}}"
         d:DesignHeight="300" d:DesignWidth="300" Header="Files">
    <TabItem.Resources>
        <converters:EnumConverter x:Key="EnumConverter" />
        <converters:SizeConverter x:Key="SizeConverter" />
    </TabItem.Resources>
    <ListView ItemsSource="{Binding Files, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}">
        <ListView.View>
            <GridView>
                <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}"
                                Width="600" />
                <GridViewColumn Header="Locale"
                                DisplayMemberBinding="{Binding Locale, Converter={StaticResource EnumConverter}}"
                                Width="300" />
                <GridViewColumn Header="Size"
                                DisplayMemberBinding="{Binding Size, Converter={StaticResource SizeConverter}}"
                                Width="200" />
            </GridView>
        </ListView.View>
    </ListView>
</TabItem>
